version: "3"
services:
  seckill_hyb:
    image: python:3.9
    working_dir: /workspace
    volumes:
    - ..:/workspace
    environment:
      JD_USER: hyb
    command:
      - bash
      - ./bin/pending.sh